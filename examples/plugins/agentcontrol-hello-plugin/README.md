# AgentControl Hello Plugin

Sample plugin that adds a `agentcall hello-plugin` command.

## Build & Install (editable)
```bash
pip install -e .
```

## Usage
```bash
agentcall hello-plugin --name VibeCoder
```
